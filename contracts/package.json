{
  "name": "safedeal-contracts",
  "version": "1.0.0",
  "description": "SafeDeal TON Smart Contracts (FunC)",
  "scripts": {
    "build": "blueprint build",
    "test": "blueprint test",
    "deploy": "blueprint run scripts/deploy.ts",
    "deploy:testnet": "blueprint run scripts/deploy.ts --testnet"
  },
  "dependencies": {
    "@ton/core": "^0.56.3",
    "@ton/ton": "^13.9.0",
    "@ton/crypto": "^3.2.0",
    "@ton/blueprint": "^0.20.0",
    "@ton/sandbox": "^0.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node": "^10.9.2",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.10",
    "ts-jest": "^29.1.1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.spec.ts"
    ]
  }
}
