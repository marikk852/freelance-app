-- ============================================================
-- Migration 030: Phase 2 — дневной потолок на streak_day
-- ============================================================
-- streak_day начисляется при открытии Mini App (1 раз в день),
-- сумма = 5×дней. Без потолка начислялся бы при каждом запросе /me.

UPDATE crystal_actions SET daily_cap = 1 WHERE key = 'streak_day';
