{
  "name": "safedeal-escrow-evm",
  "version": "1.0.0",
  "description": "SafeDeal USDT escrow for Ethereum (ERC-20) and Tron (TRC-20)",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
